Copy and Paste the folder:

The library requered for the project is JAX-WS which can also be found as .zip file in the repository!
If you are using IntelliJ IDEA you can add the library as follows:

->Extract the .zip file
->In IntelliJ IDEA navigate to File -> Project Structure -> Libraries -> Add new Java Library (press the + symbol and then choose Java) -> choose the lib folder from the extracted .zip file -> Apply

TO RUN:
-> Start TestServer.java -> Start TestService1Client.java
